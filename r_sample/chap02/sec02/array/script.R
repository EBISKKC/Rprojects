vct1 <- c(1, 2, 3, 4, 5, 6)
vct2 <- c(10, 20, 30, 40, 50, 60)
vct3 <- c(100, 200, 300, 400, 500, 600)

array1 <- array(
  c(vct1, vct2, vct3), # vct1、vct2、vct3を要素にしたベクトル
  dim=c(2,3,3)         # 行数の2、列数の3、行列の数3を要素にしたベクトル
)
